# Snippets

> TODO

## Render test — admonition + content tabs

!!! tip "Optimization note"
    Box dùng cho ghi chú tối ưu.

=== "PC"
    ```csharp
    QualitySettings.vSyncCount = 1;
    ```

=== "Mobile"
    ```csharp
    Application.targetFrameRate = 60;
    QualitySettings.vSyncCount = 0;
    ```
